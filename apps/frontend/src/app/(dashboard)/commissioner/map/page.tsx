'use client';

import { MapView } from '@/components/map/map-view';

export default function CommissionerMapPage() {
  return <MapView title="Map View" subtitle="Complaint locations, hotspots and zones across the city" />;
}
